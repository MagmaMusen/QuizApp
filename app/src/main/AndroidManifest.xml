<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="SMAP.assignment.QuizAppProject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Views.SingleQuizActivity"></activity>
        <activity android:name=".Views.PlayActivity"></activity>
        <activity android:name=".Views.EditQuestionActivity" />
        <activity android:name=".Views.QuestionsActivity" />
        <activity android:name=".Views.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.ListActivity"></activity>
        <activity android:name=".Views.SearchActivity"></activity>
    </application>

</manifest>